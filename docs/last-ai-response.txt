奖励库已完成：V2 支持稳定 `rewards`、可嵌套 `sets` 及 `{ "reward" }` / `{ "set" }` 引用；未知、循环、超深度和展开后的重复 ID 会拒绝完整重载。[RewardCatalog.java](/D:/mod/qiandao/src/main/java/dev/modmind/omnitools/config/RewardCatalog.java:25) 保持目录不可变，目录键强制作为奖励 ID，调用处不能覆盖奖励定义。

保留 V1 `templates`、`template`、`$ref`，且 V2 可安全复用旧模板。[CdkConfig.java](/D:/mod/qiandao/src/main/java/dev/modmind/omnitools/cdk/CdkConfig.java:277) 也已接入目录解析并将展开结果纳入活动指纹。文档、Schema 与测试已同步更新。

验证通过：受管构建、单元测试、隔离服务端启动、内容校验、`git diff --check` 和发布预检；发布预检仅保留“未填写更新日志”警告。