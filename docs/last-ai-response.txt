已完成成就卡片的专用 Lore 扩展：

- `GuiTextService.cardLore` 新增最大行数重载，默认仍为 6 行。
- `GuiStatusItem.create` 新增 `maximumLoreLines` 重载，默认仍为 6 行。
- 成就卡片改用最多 10 行，状态提示仍固定保留在最后。
- 称号效果预览最多显示 3 项，超出时显示“还有 N 个效果未显示”。
- 其他菜单未改变，继续使用 6 行限制。
- 新增 `GuiTextServiceTest` 覆盖默认 6 行和成就 10 行行为。

验证已通过：内容校验成功，受管构建及全部测试成功。