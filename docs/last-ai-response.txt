Placeholder API 集成已完成并验证：

- 新增稳定 `omnitools` 命名空间的 17 个只读占位符。
- 配置支持 `integrations.placeholder_api.enabled`，旧版配置默认启用。
- 未安装 Placeholder API 时不会加载第三方类；注册器已与解析器隔离。
- 占位符仅读取当前玩家数据，不触发签到、领奖、扣币或配置写入。
- README 已补充配置、使用方式、占位符列表和兼容说明。

验证结果：

- 内容校验：通过，无错误。
- Gradle 构建：通过，产物已生成。
- 服务端测试：通过，服务器稳定运行 15 秒。
- Placeholder API 环境：成功注册 17 个占位符。
- `git diff --check`：通过，无空白错误。
- 未发现 `ClassNotFoundException` 或配置回退错误。