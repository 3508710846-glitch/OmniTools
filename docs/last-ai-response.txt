这份分析与当前代码状态一致，建议优先落地两项低风险收口：

1. 将 `build.gradle` 中 JUnit 依赖收敛到 `testImplementation` / `testRuntimeOnly`。
2. 建立可重复的服务端验收脚本，覆盖 reload、模块开关、奖励账本、临时称号和 Placeholder API 缺失场景。

`ModMindEntry` 拆分和 GUI 框架迁移属于较大重构，建议在验收矩阵稳定后再实施。